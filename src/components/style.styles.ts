import styled from "styled-components";

export const FooterWrapper = styled.p`
position: absolute;
    text-align: center;
    bottom: 0;
    width: 100%;
    height: 2.5rem;
    color: #ccc;
`;
export const HeaderWrapper = styled.header`
    background-color: #39bce4;
    color: white;
    padding: 1.5vw 10vw;
    box-shadow: 0 0 10px 0 rgba(0, 0, 0, 0.3);

    h1{
        font: 2rem;
        letter-spacing: 0.1rem;
    }
`;

